local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60
 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))orfight_remains<=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot
 then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))orfight_remains<=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot
 then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60
 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))orfight_remains<=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot
 then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))orfight_remains<=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot
 then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and<25
 Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and<25
 Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and<60
 Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and>?trinket.1.cooldown.duration%2))orfight_remains<=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot
 Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and>?trinket.2.cooldown.duration%2))orfight_remains<=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot
 Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60
 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60
 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60
 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25
 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60
 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie()  then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
local function precombat() then
	if A.Flask:IsReady(unitID, true) then
		return A.Flask:Show(icon)
	end

end

local function actions() then
	if A.AutoAttack:IsReady(unitID, true) then
		return A.AutoAttack:Show(icon)
	end

	if A.RetargetAutoAttack:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.BladeDance:IsReady(unitID, true) and
	if A.PoolingForMeta:IsReady(unitID, true) and
	if A.PoolingForBladeDance:IsReady(unitID, true) and
	if A.PoolingForEyeBeam:IsReady(unitID, true) and
	if A.WaitingForMomentum:IsReady(unitID, true) and
	if A.Disrupt:IsReady(unitID, true) then
		return A.Disrupt:Show(icon)
	end

	if A.CallActionList:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.PickUpFragment:IsReady(unitID, true) and
	if A.ThrowGlaive:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
	if A.RunActionList:IsReady(unitID, true) and
end

local function cooldown() then
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Metamorphosis:IsReady(unitID, true) and Unit(unitID):TimeToDie() <25 then
		return A.Metamorphosis:Show(icon)
	end
	if A.Potion:IsReady(unitID, true) and Unit(unitID):TimeToDie() <60 then
		return A.Potion:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.1.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=2and!trinket.2.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.UseItems:IsReady(unitID, true) and Unit(unitID):TimeToDie() >?trinket.2.cooldown.duration%2))or Unit(unitID):TimeToDie() <=20)or(variable.trinket_sync_slot=1and!trinket.1.cooldown.ready)or!variable.trinket_sync_slot then
		return A.UseItems:Show(icon)
	end
	if A.SinfulBrand:IsReady(unitID, true) and
	if A.TheHunt:IsReady(unitID, true) and
	if A.ElysianDecree:IsReady(unitID, true) and
end
